<UserControl
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
  mc:Ignorable="d" 
  d:DesignHeight="768" d:DesignWidth="1366">
    <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Left">
        <Button Content="{Binding Path=player.name, Mode=OneWay, FallbackValue=Player}" Command="{Binding ClickPlayerCommand}" HorizontalAlignment="Left" Margin="0,0,0,0" VerticalAlignment="Top" Width="120" Height="150" FontSize="15"/>
        <Button Command="{Binding ClickBusinessCommand}" Width="160" Height="35" VerticalAlignment="Top" Margin="1,0,0,0">
            <StackPanel Orientation="Horizontal">
                <Label Content="Business" HorizontalAlignment="Right"/>
                <Label Content="{Binding Path=player.businesses.Count, Mode=OneWay, FallbackValue=--}" HorizontalAlignment="Left" Margin="3,0,0,0"/>
            </StackPanel>
        </Button>
    </StackPanel>
</UserControl>
