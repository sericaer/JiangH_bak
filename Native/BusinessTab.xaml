<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:noesis="clr-namespace:NoesisGUIExtensions;assembly=Noesis.GUI.Extensions"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1366">

    <Grid Background="#4C000000">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal">
            <ListView Name ="BusinessTab" ItemsSource="{Binding Path=businesses}" HorizontalAlignment="Center" Height="450" Margin="0" VerticalAlignment="Center" Width="750">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Width="120" Header="name" DisplayMemberBinding="{Binding name}"/>
                    </GridView>
                </ListView.View>
            </ListView>
            <Button Content="X" Command="{Binding ClickCloseCommand}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="1,0,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
